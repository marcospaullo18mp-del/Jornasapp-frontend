# Variáveis de Ambiente - Jornasa Frontend

# Supabase (obrigatório para auth + RLS)
VITE_SUPABASE_URL=https://SEU_PROJETO.supabase.co
VITE_SUPABASE_ANON_KEY=SEU_ANON_KEY

# API Worker (CRUD + chat)
VITE_API_URL=https://jornasa-worker.jornabot.workers.dev
VITE_ACOLHEIA_API_URL=https://jornasa-worker.jornabot.workers.dev/mensagem

# Importante: nunca coloque chaves privadas (Groq/OpenAI/etc.) em VITE_*
# Esses segredos devem ficar apenas no backend (Worker/Function)

# Google One Tap (opcional)
# VITE_GOOGLE_CLIENT_ID=
# VITE_SHOW_WORKER_EXAMPLES=1

# Use "1" apenas para rodar sem backend/Supabase
VITE_USE_LOCAL_STORE=0
